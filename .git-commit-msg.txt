fix: orden de mensajes por seq + envío a WhatsApp antes de guardar en BD

1. Trigger sek_auto_assign_seq en PostgreSQL asigna seq automáticamente
   a cualquier mensaje insertado en histcliente/histtecnico, sin importar
   el origen (webhook, agente, widget, etc.). Garantiza orden permanente.

2. chat-view.tsx ordena por seq (orden de inserción real) en lugar de
   time (que puede mentir por delay del webhook o reloj desincronizado).

3. Flujo de envío refactorizado: /api/evolution/send envía a WhatsApp
   PRIMERO y solo guarda en BD si WhatsApp confirma. Si falla, no se
   guarda nada. Elimina mensajes fantasma con UUID que nunca llegaron.

4. Botón "Reintentar" en mensajes con error de envío a WhatsApp.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
